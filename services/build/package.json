{
  "name": "@stackby/build-service",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@stackby/schema-types": "workspace:*",
    "fastify": "^4.27.0",
    "ioredis": "^5.3.2",
    "playwright": "^1.44.0",
    "zod": "^3.23.0",
    "pino": "^9.0.0",
    "p-limit": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.9.0",
    "typescript": "*",
    "vitest": "^1.5.0"
  }
}
